import type { FC } from "react";

const NavBar: FC = () => {
  return (
    <header className="h-14 border-dashed border-b-2">
      <nav className="mx-14 px-6 max-w-[50rem] h-full w-full border-dashed border-x-2"></nav>
    </header>
  );
};

export default NavBar;
